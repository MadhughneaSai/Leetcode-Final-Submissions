# 41. First Missing Positive
# Difficulty: Hard
# URL: https://leetcode.com/problems/first-missing-positive/submissions/1923724680/
# Date: 2/18/2026

# ========== Solution A ==========
# Language: txt
# Date: 2/18/2026

class Solution {class Solution {
public:public:
    int firstMissingPositive(vector<int>& nums) {    int firstMissingPositive(vector<int>& nums) {
        int n = nums.size();        int n = nums.size();
        bool contains1 = false;        bool contains1 = false;
                
        // Step 1: Replace out-of-range values with 1        // Step 1: Replace out-of-range values with 1
        for (int i = 0; i < n; i++) {        for (int i = 0; i < n; i++) {
            if (nums[i] == 1) contains1 = true;            if (nums[i] == 1) contains1 = true;
            if (nums[i] <= 0 || nums[i] > n) nums[i] = 1;            if (nums[i] <= 0 || nums[i] > n) nums[i] = 1;
        }        }
        if (!contains1) return 1;        if (!contains1) return 1;
                
        // Step 2: Mark presence at index (value - 1)        // Step 2: Mark presence at index (value - 1)
        for (int i = 0; i < n; i++) {        for (int i = 0; i < n; i++) {
            int value = abs(nums[i]);            int value = abs(nums[i]);
            nums[value - 1] = -abs(nums[value - 1]);  // Simple! No special case!            nums[value - 1] = -abs(nums[value - 1]);  // Simple! No special case!
        }        }
                
        // Step 3: First positive index means (index + 1) is missing        // Step 3: First positive index means (index + 1) is missing
        for (int i = 0; i < n; i++) {        for (int i = 0; i < n; i++) {
            if (nums[i] > 0) return i + 1;            if (nums[i] > 0) return i + 1;
        }        }
                
        return n + 1;        return n + 1;
    }    }
};};



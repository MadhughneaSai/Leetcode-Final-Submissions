# 167. Two Sum II - Input Array Is Sorted
# Difficulty: Medium
# URL: https://leetcode.com/problems/two-sum-ii-input-array-is-sorted/submissions/1923800624/
# Date: 2/18/2026

# ========== Solution A ==========
# Language: txt
# Date: 2/18/2026

class Solution {class Solution {
public:public:
    vector<int> twoSum(vector<int>& numbers, int target) {    vector<int> twoSum(vector<int>& numbers, int target) {
        int left=0;        int left=0;
        int right=numbers.size()-1;        int right=numbers.size()-1;
        int sum;        int sum;
        while(left<right){        while(left<right){
            sum=numbers[left]+numbers[right];            sum=numbers[left]+numbers[right];
            if(sum>target){            if(sum>target){
                right--;                right--;
            }            }
            if(sum<target){            if(sum<target){
                left++;                left++;
            }            }
            if(sum==target){            if(sum==target){
                return {left+1,right+1};                return {left+1,right+1};
            }            }
        }        }
        return {};        return {};
                
    }    }
};};



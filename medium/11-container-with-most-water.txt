# 11. Container With Most Water
# Difficulty: Medium
# URL: https://leetcode.com/problems/container-with-most-water/
# Date: 3/23/2026

# ========== Solution A ==========
# Language: txt
# Date: 3/23/2026

class Solution {class Solution {
public:public:
    int maxArea(vector<int>& height) {    int maxArea(vector<int>& height) {
        int left=0;        int left=0;
        int right=height.size()-1;        int right=height.size()-1;


        int max_volume=0;        int max_volume=0;
        int volume;        int volume;

        while(left<right){        while(left<right){
            if(height[left]<height[right]){            if(height[left]<height[right]){
                volume=height[left]*(right-left);                volume=height[left]*(right-left);
                left++;                left++;
            }            }
            else{            else{
                volume=height[right]*(right-left);                volume=height[right]*(right-left);
                right--;                right--;
            }            }
            max_volume=max(max_volume,volume);            max_volume=max(max_volume,volume);
        }        }

        return max_volume;        return max_volume;



